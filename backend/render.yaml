services:
  - type: web
    name: django-react-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py makemigrations
      python manage.py migrate --noinput
    startCommand: gunicorn crud.wsgi:application --bind 0.0.0.0:8000
    

# databases:
#   - name: django_react_tutorial_database
#     databaseName: django_react_tutorial_database_zox1
#     user: django_react_tutorial_database_user